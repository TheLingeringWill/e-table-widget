import { buildComponent } from '../../../plugin/componentBuilder.js';
export const checkBoxesInput = buildComponent({
    name: 'checkBoxesInput',
    attributes: {
        card: 'mode',
        checked: true
    }
});
