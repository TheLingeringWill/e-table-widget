import type { Utils } from '../../../plugin/utilities';
export declare const vercel_dialog: ({ theme, text, background, border, fontSize }: Utils) => {
    '.ui-dialog-overlay': any;
    ".ui-drawer[open][data-position='left']": {
        justifyContent: string;
    };
    ".ui-drawer[open][data-position='right']": {
        justifyContent: string;
    };
    '.ui-modal': {
        '&.ui-dialog-overlay': {};
    };
};
