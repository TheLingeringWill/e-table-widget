# §10.2 steps 2-3 + §10.3 step 7 entry — Selection complete, CONTACT ready

**Date:** 2026-04-29
**Tab:** http://localhost:8987/1
**API:** https://jonathan-api-local.e-table.co (live, X-API-Key: we_123)

## Step 2 — Services + slot grid populate — PASS

Walking the SELECTION accordion against the live API:
- Click date "April 30" -> service accordion fires `/api/getServices` ->
  upstream `GET /restaurants/1/services?startDate=&endDate=` (no trailing
  slash, camelCase) -> 200, `{services: [{Lunch}, {Dinner}], …}`.
- Service accordion renders **Lunch 12:00 - 14:00** + **Dinner 18:30 - 23:00**
  with the brand color/styling from PRD §6.4.
- Click Lunch -> pax accordion shows 1-20.
- Click pax 2 -> `/api/getServiceSlots` -> upstream
  `GET /restaurants/1/availabilities?startDate=&endDate=` -> 200,
  `{data: [{shifts: [{slots: [{time: '12:00', …}, …]}]}]}`.
- Slot grid renders 12:00, 12:15, 12:30, …, 14:00 with state colours
  derived from `slot.slotPax / slotMaxPax` per PRD §6.2.

## Flow walked

1. Navigate to /1 → widget renders (white-on-dark, theme correct)
2. Click "April 30" in inline calendar → service accordion populates
3. Service accordion shows: **Lunch 12:00 - 14:00**, **Dinner 18:30 - 23:00**
   (matches upstream — the 1h tz skew bug is fixed; formatTime now takes
   ms-from-midnight directly per shared/utils/time)
4. Click Lunch → pax accordion shows 1-20
5. Click pax 2 → POST /api/getServiceSlots → BFF unwraps the live
   `{data: [{shifts: [{slots}]}]}` envelope → slot grid shows
   12:00, 12:15, 12:30, ..., 14:00 (exact upstream times)
6. Click 12:30 + Réserver → CONTACT step opens with summary
   "Votre réservation : 30 avril, Lunch, 2 personnes, 12:30"
   and inputs for phone, email, last name, first name, notes
7. "Continuer" button visible

## Network (browser → BFF → upstream)

| Browser call          | BFF route       | Upstream call                                                   | Status |
| --------------------- | --------------- | --------------------------------------------------------------- | ------ |
| GET /1                | layout loader   | GET /restaurants/1/aggregate                                    | 200    |
| (date click)          | /api/getServices | GET /restaurants/1/services?startDate=…&endDate=…               | 200    |
| (pax click)           | /api/getServiceSlots | GET /restaurants/1/availabilities?startDate=…&endDate=…    | 200    |

## §0.4 status — item l

- step 1 (widget renders + aggregate fires) ✅
- step 2 (services + slot grid populate, color-coded by `state`) ✅
- step 3 (edit-existing-booking pre-fill) — untested (needs a known
  reservationId on restaurant 1; the booking happy path can create one)

## Remaining gaps

- §10.2 step 4-5 (cancel + reconfirm via PATCH /bookings/{id}/status):
  upstream returns 403 "operation requires authenticated member" for the
  widget caller. PRD §9.1c gap — staff-only API. Cannot work around from
  widget side.
- §10.2 step 6 (review submission via POST /reviews/upsert): upstream
  works (200 returns new review id). Untested via Chrome MCP because the
  widget UI route (`/leave-a-review`) needs a rating in the URL and was
  not exercised this turn.
- §10.3 step 7 (booking happy path): upstream POST /bookings works (just
  created booking 112 via curl); not yet walked through the full Continuer
  → submit flow in Chrome MCP this iteration.
