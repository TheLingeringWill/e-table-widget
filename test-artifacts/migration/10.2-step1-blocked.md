# §10.2 step 1 — Widget renders — BLOCKED

**Date:** 2026-04-29
**Status:** BLOCKED on PRD §9.1c (widget allow-list incomplete on upstream API)
**Tab:** http://localhost:8987/1

## Probe results (curl, X-API-Key: we_123, X-Restaurant-Id: 1)

| Endpoint                                      | HTTP | Notes |
| --------------------------------------------- | ---- | ----- |
| `GET /restaurants/1/widget`                   | 200  | OK — widget DTO returned |
| `GET /restaurants/1/aggregate`                | 403  | "operation not allowed for role widget" — §9.1c gap |
| `GET /restaurants/1/services/?...`            | 404  | Route not exposed for widget caller |
| `GET /restaurants/1/availabilities/?...`      | 404  | Route not exposed for widget caller |
| `GET /restaurants/1/review-settings`          | 403  | §9.1c gap |
| `GET /restaurants/1/bookings/1`               | 403  | §9.1c gap |
| `POST /restaurants/1/reviews/upsert`          | 405  | Method-not-allowed (route may exist but wrong shape) |
| `GET /restaurants/1/payment-intents/abc`      | 404  | Expected — §9.2 not shipped, mock handles this in BFF |

## Browser observation

Navigated to `http://localhost:8987/1` — page renders the SvelteKit error
template ("404 / Forbidden"). The BFF correctly proxies the upstream 403
to a 404 user error per `+layout.server.ts`'s `error(404, ...)` call.

## Conclusion

The widget code is fully wired against the REST adapter — the 404 is not a
widget bug. End-to-end Chrome MCP verification (§10.2 steps 1–6, §10.3
steps 7–9, §10.4, §10.5) cannot proceed until the API team fills in the
widget-caller allow-list per PRD §9.1c.

§0.4 items l/m/n/o remain failing. Per §0.3 this is a known §9 blocker; no
workaround is implemented in the widget.
