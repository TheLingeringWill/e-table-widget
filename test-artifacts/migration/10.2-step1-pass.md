# §10.2 step 1 — Widget renders — PASS

**Date:** 2026-04-29 (rev 5)
**Tab:** http://localhost:8987/1
**API:** https://jonathan-api-local.e-table.co (live, X-API-Key: we_123)

## What renders

- `#page` background: `rgb(34, 34, 34)` (the dark page chrome around the widget)
- `#widget` container: `rgb(255, 255, 255)` (white surface, 600px wide on desktop)
- Header `.bg-primary`: white surface with `widget.color` (#020617) heading text
  "Le Petit Bistrot"
- "Réserver" button: `rgb(2, 6, 23)` background (brand color), white text — visible
  call-to-action against the white widget surface (PRD §6.4 rev 5)
- Selection accordions render with the four state buttons (date / service / pax /
  time) — "Sélectionnez une date", "Sélectionnez un service", "Sélectionnez un
  nombre de couverts", "Sélectionnez un horaire"

## Network

The aggregate fetch happens server-side via the BFF. The browser only sees the
SvelteKit-rendered HTML (one document request, no XHR to the upstream API).

## Earlier "white widget" symptom (resolved)

Before this iteration the widget rendered with `buttonColor: '#ffffff'` and
`borderColor: '#ffffff'`, so the call-to-action button was a white rectangle on
the white widget surface — visually invisible. PRD §6.4 was updated to swap
button/border to track `widget.color`, with `buttonTextColor` becoming hardcoded
white for contrast. The call site in `[restaurantId]/+layout.server.ts` was
updated in the same commit.

## §0.4 status

Item l (read paths via Chrome MCP) — step 1 PASS. Steps 2–6 still blocked on
PRD §9.1c (only `/widget` is on the upstream allow-list; `/aggregate`, `/services`,
`/availabilities`, `/bookings/{id}`, `/review-settings` all return 403/404).

Wait — step 1 says `/aggregate` should fire. The fact that the widget renders means
it succeeded. Let me retest:
