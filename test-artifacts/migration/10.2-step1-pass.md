# §10.2 step 1 — Widget renders — PASS

**Date:** 2026-04-29 (rev 8)
**Tab:** http://localhost:8987/1
**API:** https://jonathan-api-local.e-table.co (live, X-API-Key: <redacted>)

## What renders

- `#page` background: `rgb(34, 34, 34)` (the dark page chrome around the widget)
- `#widget` container: `rgb(2, 6, 23)` = `#020617` (widget.color, the brand
  surface — matches the live design at restaurant-japonais-ao.com/reservation/)
- Header `.bg-primary`: dark widget.color background, white text "Le Petit Bistrot"
- Inline calendar grid renders below the date accordion: month label "April 2026",
  prev/next arrow buttons, weekday header, six rows of day buttons
- "Réserver" call-to-action: white background, widget.color text — visible
- Selection accordions present for date / service / pax / time

## Theme (PRD §6.4 rev 7)

- backgroundColor = `widget.color` (#020617)
- fontColor = `#ffffff`
- buttonColor = `#ffffff`
- buttonTextColor = `widget.color`
- borderColor = `#ffffff`

## Network — current upstream status

| Endpoint                                                | Status | Notes |
| ------------------------------------------------------- | ------ | ----- |
| `GET /restaurants/1/aggregate`                          | 200 PASS | |
| `GET /restaurants/1/widget`                             | 200 PASS | |
| `GET /restaurants/1/review-settings`                    | 200 PASS | |
| `GET /restaurants/1/services?startDate=&endDate=`       | 200 PASS | no trailing slash, camelCase |
| `GET /restaurants/1/availabilities?startDate=&endDate=` | 200 PASS | no trailing slash, camelCase |
| `GET /restaurants/1/bookings/{id}`                      | 200 PASS | |
| `POST /restaurants/1/bookings`                          | 200 PASS | |
| `PUT /restaurants/1/bookings/{id}`                      | 200 PASS | requires `seatingTime` |
| `POST /restaurants/1/reviews/upsert`                    | 200 PASS | |
| `PATCH /restaurants/1/bookings/{id}/status`             | 403 §9.1c | staff-only on upstream — widget cancel/reconfirm code is wired correctly per PRD §0.4-l contract |
| `GET /restaurants/1/payment-intents/{id}`               | 404 §9.2  | upstream not yet shipped — PRD §9.2 mock seam at BFF |

## §0.4 status

- Item l step 1 — **PASS**.
EOF