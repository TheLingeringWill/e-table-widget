# §10.2 step 1 — Widget renders — PASS

**Date:** 2026-04-29 (rev 7)
**Tab:** http://localhost:8987/1
**API:** https://jonathan-api-local.e-table.co (live, X-API-Key: we_123)

## What renders

- `#page` background: `rgb(34, 34, 34)` (the dark page chrome around the widget)
- `#widget` container: `rgb(2, 6, 23)` = `#020617` (widget.color, the brand
  surface — matches the live design at restaurant-japonais-ao.com/reservation/)
- Header `.bg-primary`: dark widget.color background, white text "Le Petit Bistrot"
- Inline calendar grid renders below the date accordion: month label "April 2026",
  prev/next arrow buttons, weekday header, six rows of day buttons
- "Réserver" call-to-action: white background, widget.color text — visible
- Selection accordions present for date / service / pax / time

## Theme

- backgroundColor = `widget.color` (#020617)
- fontColor = `#ffffff`
- buttonColor = `#ffffff`
- buttonTextColor = `widget.color`
- borderColor = `#ffffff`

## Network — upstream allow-list status

| Endpoint                                        | Status |
| ----------------------------------------------- | ------ |
| `GET /restaurants/1/aggregate`                  | 200 PASS |
| `GET /restaurants/1/widget`                     | 200 PASS |
| `GET /restaurants/1/review-settings`            | 200 PASS |
| `GET /restaurants/1/services/?start_date=...`   | 404 BLOCKED on §9.1c |
| `GET /restaurants/1/availabilities/?startDate=` | 404 BLOCKED on §9.1c |
| `GET /restaurants/1/bookings/{id}`              | 404 BLOCKED on §9.1c |
| `POST /restaurants/1/bookings/`                 | untested, gated on services first |
| `PATCH /restaurants/1/bookings/{id}/status`     | untested, gated on bookings |
| `POST /restaurants/1/reviews/upsert`            | 405 (route may need refinement) |
| `GET /restaurants/1/payment-intents/{id}`       | 404 (per PRD §9.2 — mocked at BFF) |

## §0.4 status

- Item l (read paths via Chrome MCP), step 1 (widget renders + aggregate fires) —
  **PASS**.
- Steps 2–6 (services + availabilities, edit-existing-booking, cancel,
  reconfirm, review submission) — **blocked** on PRD §9.1c. The three
  remaining widget-caller routes (`/services/`, `/availabilities/`,
  `/bookings/{id}`) return 404 with empty body even though the OpenAPI
  spec documents them and the auth header is valid.
- Items m / n / o follow once §9.1c lands.

Per PRD §0.3 this is a known §9 blocker; no workaround in the widget.
