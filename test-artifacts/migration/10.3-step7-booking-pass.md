# §10.3 step 7 — Booking happy path (no deposit) — PASS

**Date:** 2026-04-29
**Tab:** http://localhost:8987/1
**API:** https://jonathan-api-local.e-table.co (live, X-API-Key: <redacted>)

## Flow walked end-to-end via Chrome MCP

1. Date pick: April 30 → SELECTION shows Lunch 12:00–14:00 + Dinner 18:30–23:00
2. Service: Lunch → pax accordion populates
3. Pax: 2 → slot grid shows 12:00, 12:15, 12:30, ..., 14:00
4. Slot: 12:30 + Réserver → CONTACT step opens with summary
   "Votre réservation : 30 avril, Lunch, 2 personnes, 12:30"
5. Form fill: phone +33612345678, email test@example.com, last name TestCancel
6. Continuer → POST /api/book → BFF posts to upstream
   POST /restaurants/1/bookings → 200, booking id returned, no deposit
   required (lunch service has captureEnabled: false)
7. DONE step renders:
   "Votre réservation du Thursday 30 April à 12:30 pour 2 personnes a été reçue.
    Un email vous a été envoyé à test@example.com."

## Network: BFF → upstream

| BFF call         | Upstream              | Status |
| ---------------- | --------------------- | ------ |
| POST /api/book   | POST /restaurants/1/bookings | 200 |

## §0.4 status

- Item m (booking write path via Chrome MCP, no deposit) — **PASS**.
- Step 8 (booking with deposit, Stripe Elements + §9.2 mock) — untested
  this turn. Restaurant 1's Lunch service has captureEnabled: false; would
  need a service with captureEnabled: true to exercise the mock.
- Step 9 (standalone payment route) — untested for the same reason.

## Step 6 — Review submission — PASS

(Per PRD §10.2 step 6 — recorded here because it was tested in the same
Chrome MCP session as the booking happy path.)

Navigated to /1/leave-a-review?rating=5, filled comment "Excellent restaurant!",
clicked Envoyer. POST /api/<form-action> → upstream POST /restaurants/1/reviews/upsert
→ "Merci pour votre avis !" rendered.

## Notes / drift

- The leave-a-review page hardcodes the "Nous regrettons que votre expérience
  n'ait pas été à la hauteur" copy regardless of rating; pre-existing behaviour
  in the template, not regression. PRD §6.4's reviewUrl mapping returns null
  for restaurant 1 (the API doesn't expose `reviewUrl` on /aggregate's
  review block), so the high-rating Google Maps redirect doesn't trigger.
