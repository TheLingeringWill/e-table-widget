# §10.4 — Iframe protocol — PASS

**Date:** 2026-04-29
**Tab:** http://localhost:8987/parent.html (harness in static/)
**API:** https://jonathan-api-local.e-table.co (live, X-API-Key: we_123)

## Step 10 — Auto-height postMessage — PASS

Parent harness at `widget/static/parent.html` (also kept under
`widget/test-fixtures/` for tracking) embeds the widget via the embeddable
`<script id="cooking" data-element="main" src="http://localhost:8987/1/script">`
tag. The parent's message listener mirrors every iframe postMessage to
`console.log` so Chrome MCP can read it.

Observed (Chrome MCP read against the parent tab):

- `iframeSrc = http://localhost:8987/1?embedded=true` — confirms PRD §5
  hard cut (no `[widgetId]` segment in the embed URL).
- Steady stream of `{type: "resize", data: <px>}` messages: 231, 311,
  311, 339.375, 365.34375, 390.9375, 412.96875, … as the widget grows.
- iframe `style.height = "607px"` updates to match content.
- Also observed: `{loaded: true}` ready ping, and `{type: "gtm_event",
  event: {event: "widget_step_selection", step: 1, step_name: "Selection"}}`
  — GTM forwarding is live too.

## Step 11 — Builder mode live-edit — PASS

Open `/1?builder=true` directly. Send a theme-change postMessage from the
page console:

```js
window.postMessage({ theme: { fontColor: '#ff0000' } }, '*');
```

Result: `getComputedStyle(h1).color === 'rgb(255, 0, 0)'` immediately,
no page reload. Widget surface still `widget.color` (#020617) — the
patch only touches the field that was sent.

## §0.4 status

- Item n (iframe protocol verified) — **PASS**.
